#ifndef SIGHANDLER_HPP
# define SIGHANDLER_HPP

# include <csignal>
# include "utils.h"

using std::string;

class SigHandler
{
	private:
        // struct sigaction sa;
		// void handleSignal(int signum);
	public:
		// int initHandler();
		// ~SigHandler();

};

#endif
